# Core analysis. The geometry and probe modules depend on numpy and pandas only,
# deliberately: they must be importable and testable without a GPU, without
# torch, and without network access. Keep it that way — the unit tests in tests/
# are the thing standing between a null result and an uninterpretable one, and
# they should run anywhere.
numpy>=1.26,<3
pandas>=2.1,<3
pyarrow>=15.0          # parquet for the statement index; CSV works but is slow at scale
scipy>=1.11            # used by the subspace and regression reporting paths

# Model access. Needed only by scripts/02_extract.py and src/pivot/.
torch>=2.2
transformers>=4.44     # 4.44+ for output_hidden_states behaviour assumed in extract.py
accelerate>=0.30       # device_map in 02_extract.py
sentencepiece>=0.2     # Gemma and Aya tokenizers
protobuf>=4.25         # sentencepiece dependency, pinned to avoid the 3.x/4.x break

# Data construction.
requests>=2.31         # Wikidata SPARQL endpoint
PyYAML>=6.0            # configs/

# RQ2 covariates. lang2vec is optional and the code raises rather than silently
# substituting a placeholder when it is missing — a dropped regressor must be a
# visible decision, not an accident.
lang2vec>=1.2 ; python_version < "3.13"

# Plotting, for the layer profiles and the dissociation scatter.
matplotlib>=3.8

# Development.
pytest>=8.0
ruff>=0.5

# ---------------------------------------------------------------------------
# NOT pinned exactly, and why: this is a research repo where the analysis is the
# artefact, not the environment. What must be reproducible is the numbers, and
# those are protected by seeds and by the recorded configs in meta.json /
# manifest_*.json rather than by a lockfile. If the paper is accepted, freeze
# with `pip freeze > requirements.lock.txt` at submission and ship both.
#
# lang2vec's Python 3.13 marker: the package has historically lagged on new
# Python releases. If it will not install, do NOT drop the typological-distance
# regressor silently — substitute a documented hand-coded distance matrix and say
# so in the paper.
#
# Not included and deliberately so:
#   scikit-learn — the probes here are mass-mean and LDA, both a few lines of
#     numpy. Pulling in sklearn invites someone to swap in a regularised
#     classifier whose direction is no longer the difference of class means,
#     which would silently break the RQ1 geometry (see probes/mass_mean.py on
#     why raw and whitened directions are not interchangeable).
#   datasets — MultiLoKo is gated and downloaded manually; adding the HF datasets
#     dependency for one gated download is not worth the surface area.
# ---------------------------------------------------------------------------
